<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vanrui.app.user.dao.FreezeDao">
 
      
  <insert id="insert"  parameterType="com.vanrui.app.user.dto.FreezeDto"  useGeneratedKeys="true" keyProperty="id">
	    insert   into  t_frozen_history ( F_UID,F_TYPE,SOLUTION,OPERATOR,OPERATE_TIME )
	    values (
	    	#{fUId},
	    	#{fType},
	    	#{solution},
	    	#{operator},
	    	#{operateTime}
	    )
	    	 
  </insert>
  
   
</mapper>