<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vanrui.app.user.dao.RoleWebmenuDao">
   
  <insert id="insertAuth"  parameterType="java.util.List"  >
	    insert   into  T_REF_WEBSOURCE_ROLE(R_ID,S_ID)
	    values
	    <foreach collection="authList" item="item" index="index" separator=",">
	    (
	        #{item.rId},
	        #{item.sId}
	    ) 
	    </foreach>
	    
  </insert>
  
   <delete id="deleteAuth"  parameterType="java.lang.Long" >
  		delete  from  T_REF_WEBSOURCE_ROLE  where  R_ID = #{roleId}
  </delete>
  
</mapper>